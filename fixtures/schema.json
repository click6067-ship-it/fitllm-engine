{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "fitllm measured fixture",
  "description": "One real-world measurement: what a model ACTUALLY consumed on real hardware. Submitted via PR; cross-referenced by the Fit Census as predicted-vs-measured.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["model", "device", "quant", "ctx", "kvBits", "measuredPeakGB", "runtime", "source", "date"],
    "properties": {
      "model": { "type": "string", "description": "Exact engine model name, e.g. 'GLM-4.7-Flash'" },
      "device": { "type": "string", "description": "Exact census device name, e.g. 'RTX 4090' or 'M5 Max 64GB'" },
      "quant": { "type": "string", "description": "GPU: Q4_K_M/Q5_K_M/Q6_K/Q8_0/FP16 · Mac: 4bit/8bit/16bit" },
      "ctx": { "type": "integer", "minimum": 1024, "description": "Context length actually loaded" },
      "kvBits": { "type": "integer", "enum": [16, 8, 4] },
      "measuredPeakGB": { "type": "number", "exclusiveMinimum": 0, "description": "Peak memory actually observed (GB)" },
      "runtime": { "type": "string", "description": "llama.cpp <version> / ollama <version> / mlx <version> / vllm <version> — version required for reproducibility" },
      "source": { "type": "string", "description": "URL of the PR or issue containing the full report" },
      "date": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
      "notes": { "type": "string" }
    }
  }
}
